[build]
builder = "dockerfile"

[deploy]
startCommand = "python manage.py migrate && python manage.py collectstatic --noinput && gunicorn config.wsgi:application --bind 0.0.0.0:$PORT --workers 3"

[env]
DEBUG = "False"
ALLOWED_HOSTS = "*.railway.app"